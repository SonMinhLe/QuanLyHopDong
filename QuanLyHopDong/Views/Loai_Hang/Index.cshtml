@model IEnumerable<QuanLyHopDong.Models.Loai_Hang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ten_Loai_Hang)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten_Loai_Hang)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Loai_Hàng }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Loai_Hàng }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Loai_Hàng })
        </td>
    </tr>
}

</table>
